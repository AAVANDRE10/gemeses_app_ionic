import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Apresentacao_de_localidade',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
